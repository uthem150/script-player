# 구현 로드맵

[설계 스펙](docs/superpowers/specs/2026-09-10-script-player-design.md) 을 8 단계로 쌓는다.

**원칙**

- 각 단계는 **테스트가 통과하고 화면을 눈으로 확인한 뒤** 다음으로 넘어간다
- 구현 코드보다 테스트를 먼저 쓴다 (TDD — Red → Green → Refactor)
- 단계 안에서도 커밋을 쪼갠다. 한 커밋이 한 가지만 바꾸게
- 위험한 미지수는 **앞으로 당긴다**. 뒤에서 뒤집히면 손해가 크다

---

## 1단계 · 환경과 미지수 검증

이 앱에서 확인되지 않은 것이 세 가지다. 본 구현 전에 실측한다.

**목표**

- Android SDK · JDK 21 설치, Gradle 프로젝트 골격
- 화면 하나뿐인 검증 앱으로 세 가지 실측

**실측할 것**

| 미지수 | 확인 방법 | 뒤집히면 |
| --- | --- | --- |
| 오프라인 한국어 음성 존재 | `TextToSpeech.voices` 필터링 결과를 화면에 나열 | 안내 화면 설계가 필수 경로로 승격 |
| `synthesizeToFile` 실속도 | 10분 분량 대본을 합성해 소요 시간 측정 | 스트리밍 시작 전략 재검토 |
| 에어팟 탭 수신 | `MediaSessionService` + 무음 WAV 재생, 탭 이벤트 로깅 | 재생 구조 전면 재설계 |
| `onRangeStart` 수신 여부 | 합성 중 콜백을 찍어 글자 오프셋·프레임이 오는지 확인 | 단어 탭 재생이 정확값 대신 추정으로, 단어 하이라이트는 포기 |

**완료 조건**

- [x] `./gradlew assembleDebug` 로 APK 가 나옴 (14MB)
- [x] 폰에 설치되어 실행됨 (Galaxy S23+ · Android 16)
- [x] 네 미지수의 실측값이 스펙 §12 에 반영됨 — 넷 다 통과
- [x] `spike-2` 로 에어팟 2탭 문장 이동 확인 + 문장 41개 플레이리스트가 매끄럽게 이어짐
- [x] 나머지(화면 끈 뒤 재생 · 비행기 모드 · 잠금화면)는 8단계 체크리스트로 이관 — 근거는 스펙 §12
- [x] `spike` 패키지 삭제 — 산출물은 측정값이지 코드가 아니다

**커밋 분할**

1. ~~`chore: Gradle · Android 프로젝트 골격`~~
2. ~~`spike: 기기 TTS 음성 목록과 합성 속도 측정`~~
3. ~~`spike: 미디어 버튼 수신 확인과 자기보고 화면`~~
4. ~~`docs: 1단계 실측 결과를 스펙에 반영`~~
5. ~~`spike: 문장 플레이리스트로 재측정 + 시스템 바 인셋 수정`~~
6. ~~`chore: 검증 앱 제거`~~

---

## 2단계 · 디자인 시스템과 스크린샷 파이프라인

**파이프라인을 먼저 세운다.** 이 단계가 끝나면 이후 모든 화면이 자동으로 검증 대상이 된다.
나중에 붙이면 이미 쌓인 화면을 되짚어야 한다.

**목표**

- Compass 3계층 토큰 이식 — `Primitive.kt` → `Alias.kt` → `Semantic.kt` → `Theme.kt`
- Pretendard 번들, 폰용 타입 스케일 (본문 15sp, 대본 18sp/행간 1.7)
- 기본 컴포넌트: `Button`, `IconButton`, `Card`, `Slider`, `Chip`, `BottomSheet`, `TextField`
- Roborazzi 설정, `updateReadme` Gradle 태스크

**완료 조건**

- [x] 밝게·어둡게 두 테마에서 토큰 카탈로그 PNG 가 기록됨 (색·글자 4장)
- [x] `./gradlew verifyRoborazziDebug` 가 회귀를 잡아냄 (Blue500 을 일부러 틀어 확인)
- [x] `./gradlew updateReadme` 가 README 의 `SCREENS` 블록을 다시 씀
- [x] 3계층 중 **어느 컴포넌트도 1·2계층을 직접 참조하지 않음** (소스를 훑는 테스트로 단언)
- [x] `./gradlew check` 한 줄에 유닛 테스트·스크린샷 회귀·Lint 가 다 묶임
- [x] Pretendard 가변 폰트 번들 (OFL 전문 포함)
- [x] 기본 컴포넌트 — 단추 2종·카드·칩·입력칸

**커밋 분할**

1. ~~`feat: 3계층 색 토큰과 테마 이식`~~ (창 배경 버그 수정 포함)
2. ~~`test: Roborazzi 스크린샷 기록·검증 파이프라인`~~
3. ~~`build: README 화면 블록 생성 태스크`~~
4. ~~`feat: Pretendard 가변 폰트`~~
5. ~~`feat: 기본 컴포넌트`~~

**컴포넌트를 7종에서 5종으로 줄였다.** Material3 가 이미 우리 `colorScheme` 을 쓰므로
래퍼는 **결정을 박아두는 것**만 만들었다 — 48dp 최소 높이, 그림자 대신 경계선, 배속 칩.
`IconButton` 은 아이콘 자산이, `Slider`·`BottomSheet` 는 동작이 정해지는 화면 단계에서
붙인다. 미리 만들면 쓰이지 않는 모양으로 굳는다.

**순서를 바꿨다.** 컴포넌트보다 파이프라인을 먼저 세웠다 — Robolectric 이 SDK 37 과
붙는지가 이 단계의 위험이었고, 안 붙으면 스크린샷 전략을 손봐야 했다. 붙었다.

---

## 3단계 · 파서 (TDD)

이 앱의 실제 값이다. 순수 Kotlin, 안드로이드 의존 0. 가장 촘촘히 덮는다.

**목표**

- `parseScript(raw): ParsedScript`
- 마크다운 정리, 화자 라벨 검출, 문장 분할, `sourceRange` 보존

**완료 조건**

- [x] 스펙 §5.1 의 마크다운 표 항목이 **전부** 테스트로 있음
- [x] 화자 검출: 2인 대화 · `**이름**:` · 전각 콜론 · **1회만 나온 라벨은 화자 아님** · 12자 초과
- [x] 문장 분할: 종결부호 · 소수점 · 약어 · 말줄임표 · 200자 보조 분할 · 빈 문장
- [x] `sourceRange` 가 원문 오프셋과 일치함을 단언
- [x] 실제 대본 픽스처로 회귀 방지 (앱이 쓰는 `sample-script.md` 를 그대로 읽는다)
- [x] 파서 테스트 60개 · 안드로이드 의존 0

**커밋 분할**

1. ~~`feat: 문장 나누기와 원문 오프셋 보존`~~ (오프셋을 처음부터 구조에 넣었다)
2. ~~`feat: 마크다운 마커 제거`~~
3. ~~`feat: 코드블록·표 제거`~~
4. ~~`feat: 화자 라벨 검출`~~
5. ~~`feat: 문장 분할 예외 처리`~~
6. ~~`test: 실제 대본 픽스처로 회귀 방지`~~

**오프셋을 마지막이 아니라 첫 커밋에 넣었다.** 나중에 붙일 수 있는 성질이 아니다 —
마크다운을 벗기며 문자열을 치환하면 위치가 밀려, 그 뒤에 오프셋을 되살릴 방법이 없다.

---

## 4단계 · 보관함

**목표**

- Room `ScriptEntity`, `Flow<List<...>>` 노출
- 대본 추가·수정·이름 변경·삭제
- `ACTION_SEND` 수신 + 확인 시트
- `LibraryScreen`

**완료 조건**

- [x] 인메모리 Room 으로 CRUD 테스트 (13개)
- [x] 빈 상태 · 목록 상태 스크린샷 (밝게·어둡게)
- [x] 긴 제목, 진도 0%·중간·100% 가 픽스처에 있음
- [x] 공유받은 글의 미리보기를 순수 함수로 (테스트 7개)
- [ ] 실제로 다른 앱에서 공유해 담기 → 8단계 실기기 체크리스트로

**커밋 분할**

1. ~~`feat: Room 대본 보관함`~~ (CRUD 포함)
2. ~~`feat: 보관함 화면`~~
3. ~~`feat: 다른 앱에서 텍스트 공유받기`~~

이름 변경·삭제를 따로 커밋하지 않고 보관함에 함께 넣었다. 저장 계층과 화면이 한 벌로
움직여야 눌러 볼 수 있는 상태가 되고, 나눠 놓으면 중간 커밋이 화면 없는 기능이 된다.

**카드 탭은 아직 잇지 않았다.** 재생기가 7단계라, 눌러도 아무 일 없는 자리를 만드는 대신
`⋯` 에 관리 기능만 달았다.

---

## 5단계 · 합성 파이프라인

**목표**

- `Synthesizer` 인터페이스와 `TextToSpeech` 구현
- 콘텐츠 주소 캐시 — `sha256(text + voice + pitch + engine)`
- 500MB 상한 LRU 정리
- `Flow<SynthesisProgress>`

**완료 조건**

- [x] 캐시 키가 안정적임 (같은 입력 → 같은 파일명) 테스트
- [x] 문장·음성·피치·엔진 중 하나만 달라도 키가 달라짐 — 재합성 범위의 근거
- [x] LRU 경계 테스트
- [x] 오프라인 음성이 없을 때 안내 화면 (스크린샷 2장)
- [x] **합성은 rate 1.0** — 속도를 파일에 굽지 않음
- [x] 단어 타이밍이 오디오와 **한 쌍으로** 저장·삭제됨
- [x] `atMs = frame * 1000 / sampleRate` 환산 테스트 (sampleRate 는 WAV 헤더에서 청크를 훑어 읽음)
- [x] 타이밍이 안 오는 엔진에서도 합성이 정상 완료됨 (빈 목록으로)
- [x] 음성 필터 세 조건 — `-local`/`-network` 짝, `kok` 오검출까지
- [x] 한 문장이 실패해도 나머지를 계속 합성
- [x] 첫 문장이 전체 합성을 기다리지 않고 나옴 (스트리밍 시작의 근거)

**커밋 분할**

1. ~~`feat: 콘텐츠 주소 오디오 캐시`~~ (음성 필터·화자 배정 포함)
2. ~~`feat: TextToSpeech 어댑터와 합성 큐`~~ (단어 타이밍 수집 포함)
3. ~~`feat: 오프라인 음성 없을 때 안내`~~

타이밍 파일은 스펙의 `.json` 대신 **`.timings` 평문**으로 뒀다. 정수 세 개가 한 줄인
표에 JSON 파서를 끌어올 이유가 없고, 의존성이 하나 줄었다.

`AndroidSynthesizer` 는 실제 `TextToSpeech` 를 쓰므로 유닛 테스트로 덮지 못한다.
같은 API 호출을 1단계 검증 앱에서 실기기로 확인했고, 조율 로직은 가짜 합성기로 덮었다.

---

## 6단계 · 재생

**목표**

- `PlaybackService : MediaSessionService`
- 합성 완료 순서대로 플레이리스트에 붙이는 스트리밍 시작
- 실시간 속도·음높이 (`PlaybackParameters`)
- 시크, 문장 이동, 진도 누적합
- 이어듣기 지점 저장

**완료 조건**

- [x] 첫 문장 합성 직후 재생이 시작됨
- [x] fake `PlayerController` 로 조율 로직 테스트 (뒤섞여 와도 순서 유지)
- [x] 진도 누적합이 추정 → 실측으로 교체되며 **뒤로 튀지 않음**
- [x] 이어듣기 자리가 붙을 때까지 재생을 미룸 — 20번부터 들어야 하는데 0번에서
      재생하면 이어듣기가 무의미해진다
- [ ] 실기기: 화면 끄고 연속 재생, 에어팟 1·2·3탭, 잠금화면 시크 → 8단계
- [ ] 실기기: 재생 중 속도 변경 즉시 반영 + 피치 유지 → 8단계
- [ ] 실기기: 전화 오면 멈추고 끊으면 재개 → 8단계

**커밋 분할**

1. ~~`feat: 재생 — MediaSessionService·진도 누적합·조율`~~
2. ~~`feat: 재생기 화면과 앱 연결`~~

**live `speak()` 폴백 모드는 만들지 않는다.** 실측 기기에서 `synthesizeToFile` 이
확인됐고, 폴백은 시크·실시간 속도가 없는 두 번째 재생 경로를 통째로 더하는 일이다.
가지고 있지도 않은 기기를 위해 그 복잡도를 지금 지불하지 않는다 — 필요한 기기가
나타나면 그때 만든다.

---

## 7단계 · 재생기 화면

**목표**

- 원문 표시 + 현재 문장 하이라이트 + 자동 스크롤
- **아무 단어나 탭하면 그 지점부터 재생** (길게 누르면 시스템 텍스트 선택)
- 단어 타이밍이 있으면 현재 단어도 하이라이트
- 손 스크롤 시 자동 스크롤 중단 + "현재 위치로"
- 하단 컨트롤: 10초 뒤로 · 이전 문장 · 재생/정지 · 다음 문장 · 10초 앞으로
- 시크바, 속도 칩 + 슬라이더, 합성 진행 표시

**완료 조건**

- [x] 재생 중 · 정지 · 합성 중 스크린샷 (밝게·어둡게)
- [x] 화자 4명, 매우 긴 문장 하나, 실패한 문장 하나가 픽스처에 있음
- [x] 탭 좌표 → 글자 오프셋 → 시각 환산 단위 테스트 (타이밍 있는 경우와 없는 경우)
- [x] 하이라이트가 커지지 않음 — 배경과 굵기로만 짚는다
- [x] 터치 대상 48dp 이상
- [ ] 실기기: 단어를 탭해 그 지점부터 재생되는지 → 8단계
- [ ] 실기기에서 걸으면서 조작 가능한지 → 8단계
- [ ] 손 스크롤 시 자동 스크롤 중단 + "현재 위치로" — 아직 안 만듦

**커밋 분할**

1. `feat: 대본 본문과 현재 문장 하이라이트`
2. `feat: 단어 탭으로 그 지점부터 재생`
3. `feat: 재생 중 현재 단어 하이라이트`
4. `feat: 자동 스크롤과 손 스크롤 중단`
5. `feat: 재생 컨트롤과 시크바`
6. `feat: 속도 조절 UI`
7. `feat: 합성 진행 표시`
8. `test: 재생기 화면 스크린샷`

---

## 8단계 · 설정과 마무리

**목표**

- 음성 선택, 화자별 음성·음높이 배정, 전역 음높이
- 캐시 사용량·비우기, 테마
- 실기기 체크리스트 완주, README 생성

**완료 조건**

- [ ] 화자 배정 변경 → 해당 문장 재합성 → 진도 유지 (실기기)
- [ ] 스펙 §7.3 실기기 체크리스트 **전 항목** 통과
- [ ] **가변 폰트 굵기 확인.** 스크린샷에서 SemiBold 제목 중 숫자 뒤부터 더 굵게 렌더됐다
      (`…과정과 왜 16밀리초를…`). 원문에 마크다운이 없고 한 문자열이므로 Robolectric 의
      가변 폰트 렌더 문제로 보이나, 실기기에서 같은지 확인해야 한다. 실기기에서도 그러면
      정적 굵기 파일로 바꾼다
- [ ] `./gradlew check` 전부 통과
- [ ] `./gradlew updateReadme` 로 README 화면 블록이 채워짐
- [ ] 합성 실측 속도를 README 나 스펙에 기록

**커밋 분할**

1. `feat: 음성 선택과 화자별 배정`
2. `feat: 캐시 관리와 테마 설정`
3. `docs: 실기기 검증 결과 기록`
4. `docs: 화면 스크린샷으로 README 갱신`

---

## 진행 상황

| 단계 | 상태 |
| --- | --- |
| 1 · 환경과 미지수 검증 | **완료** — 실측 결과는 스펙 §12 |
| 2 · 디자인 시스템·스크린샷 | **완료** |
| 3 · 파서 | **완료** — 테스트 60개 |
| 4 · 보관함 | **완료** — 테스트 101개 누적 |
| 5 · 합성 파이프라인 | **완료** — 테스트 145개 누적 |
| 6 · 재생 | **완료** |
| 7 · 재생기 화면 | **완료** — 테스트 192개 누적 |
| 8 · 설정과 마무리 | 다음 |
