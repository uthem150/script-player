<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      창 배경. Compose 가 그리기 전과 시스템 바 자리에 비치는 색이라, 3계층의
      background 와 같은 값이어야 한다 (밝은 테마 = Gray50).
      어긋나면 화면 위쪽만 색이 다르게 보인다 — 실제로 그랬다.
    -->
    <color name="window_background">#F9FAFB</color>
</resources>
